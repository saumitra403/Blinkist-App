import { createTheme } from '@mui/material/styles';

const mainTheme = createTheme({
    palette: {
        background: {
            default: "#FFFFFF"
        },
        primary: { 
            main: '#ffffff',
        }
    }
})

export default mainTheme;